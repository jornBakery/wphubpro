{
    "name": "stripe-get-subscription",
    "version": "1.0.0",
    "description": "Appwrite function to get a user's Stripe subscription status.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "node-appwrite": "^22.1.3",
        "stripe": "^20.4.1"
    }
}
