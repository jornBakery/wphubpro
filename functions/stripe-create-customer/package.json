{
  "name": "stripe-create-customer",
  "version": "1.0.0",
  "description": "Appwrite function to create a Stripe customer on new user creation.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-appwrite": "^12.0.1",
    "stripe": "^14.10.0"
  }
}
