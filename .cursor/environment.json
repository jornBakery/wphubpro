{
  "install": "npm ci && for dir in functions/*; do if [ -f \"$dir/package.json\" ]; then if [ -f \"$dir/package-lock.json\" ]; then npm ci --prefix \"$dir\"; else npm install --no-package-lock --no-audit --no-fund --prefix \"$dir\"; fi; fi; done"
}
